Fitness App project with React Native use with (Create React Native App)[https://github.com/react-community/create-react-native-app].

1. `npm install` or `yarn install`
2. `npm start` or `yarn start`
